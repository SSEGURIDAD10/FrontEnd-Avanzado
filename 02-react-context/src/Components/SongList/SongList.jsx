import './songList.css'

const SongList = () => {
  return (
    <section className='row-container'>
      {/* {
        loading
        ? <h2>Cargando . . .</h2>
        : filteredSongList.map((song) => {
          <article>
            <h3>{title}</h3>
            <h3>{artist}</h3>
          </article>
        })
      } */}
    </section>
  )
}

export default SongList